/*
조회 : SELECT
테이블 선택 : FROM
내부 조인 : (INNER) JOIN
조인 조건 : ON
조건 : WHERE
AND : 모두 참일 경우
정렬 : ORDER BY
내림차순 : DESC
*/

SELECT FH.FLAVOR
FROM FIRST_HALF FH
     JOIN ICECREAM_INFO II
     ON FH.FLAVOR = II.FLAVOR
WHERE FH.TOTAL_ORDER > 3000 && II.INGREDIENT_TYPE = 'fruit_based'
ORDER BY FH.TOTAL_ORDER DESC
