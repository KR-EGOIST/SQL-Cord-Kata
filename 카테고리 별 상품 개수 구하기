/*
조회 : SELECT
특정 갯수 글자만 출력 : LEFT(컬럼명, 왼쪽부터 가져올 갯수)
별칭 : AS 컬럼
테이블 선택 : FROM
그룹 : GROUP BY
정렬 : ORDER BY
*/

SELECT LEFT(PRODUCT_CODE, 2) AS CATEGORY, COUNT(PRODUCT_ID) AS PRODUCTS
FROM PRODUCT
GROUP BY CATEGORY
ORDER BY CATEGORY
