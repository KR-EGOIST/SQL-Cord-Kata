STUDENTS에서 75점 이상 득점한 학생의 이름을 질의합니다.
각 이름의 마지막 세 글자로 출력을 정렬합니다.
두 명 이상의 학생이 모두 같은 마지막 세 글자로 끝나는 이름을 가지고 있는 경우(예: Bobby, Robby 등), ID를 오름차순으로 2차 정렬합니다.

------------------------------------

SELECT
    Name
FROM
    students
WHERE
    Marks > 75
ORDER BY
    RIGHT(name, 3),
    ID
